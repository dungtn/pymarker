pymarker
========

A Python object marker based on OpenCV.

## Usage

- `ESC` to stop
- `s` to skip image
- `left mouse button` to mark object using box
- `right mouse button` to mark object using line
- `d` to delete last mark
- `r` to remove last label
- `0-9` to label last marked object
- `SPACE` to save image and next
